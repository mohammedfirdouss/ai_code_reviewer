name = "ai-code-reviewer"
main = "src/index.ts"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]
# Account ID will be read from CLOUDFLARE_ACCOUNT_ID environment variable
# No need to hardcode account_id here

# AI binding configuration
[ai]
binding = "AI"
# Set to true to suppress warnings in development
# remote = true

# Durable Objects configuration
[[durable_objects.bindings]]
name = "CODE_REVIEWER_AGENT"
class_name = "CodeReviewerAgent"
script_name = "ai-code-reviewer"

# Migration for Durable Objects (Free plan compatible)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["CodeReviewerAgent"]
