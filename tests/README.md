# Tests Directory

This directory contains test files and utilities for testing the application.

## Files

- **`test-websocket.html`** - Standalone HTML file for testing WebSocket connections to the Worker

## Usage

Open `test-websocket.html` in a browser to test WebSocket functionality without running the full frontend application.
